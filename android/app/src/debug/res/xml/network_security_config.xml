<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEBUG ONLY: permit cleartext HTTP so the app can reach the local dev
         backend over the LAN (emulator host 10.0.2.2 or the dev PC's Wi-Fi IP).
         This file overrides the HTTPS-only main config for debug builds only;
         release builds never permit cleartext. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
